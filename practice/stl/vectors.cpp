#include <iostream>
#include <vector>
using namespace std;

int main (int argc, char *argv[]){
    // TODO:
    // QUES:
    // 1. Perform Linear Search on a Vector
    // 2. Define a function to reverse the vector(both pass by reference and pass by value)
    // 3. Does pass by value change the original vec? Why not?
    return 0;
}
